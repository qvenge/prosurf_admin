# API Configuration
# Production API URL (used in production builds)
# In development, requests are proxied through /api to avoid CORS issues
VITE_API_URL=https://qvenge.tw1.ru

# Timezone for displaying dates/times (IANA timezone identifier)
# Default: Europe/Moscow (UTC+3)
VITE_TIMEZONE=Europe/Moscow